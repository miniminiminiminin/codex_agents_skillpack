# Local Memory Stub

`memory/` is intentionally reduced to a public stub in this package.

- Keep project-specific notes outside the reusable skillpack.
- Do not store normative operating rules here.
- If a rule changes shared agent behavior, encode it in `AGENTS.md`, `graph/`, `prompts/`, `templates/`, or `skills/`.
