# Active Slice Note

Use `.codex/templates/active-slices.md` as the reusable source template.

Do not keep current-project slice boards in `.codex`.

Project-instance boards belong under `docs/operations/`, for example:

- `docs/operations/active-slices.md`

This note is local-only and non-authoritative.
