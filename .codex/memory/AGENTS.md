# Local Memory Stub

This directory exists only to satisfy graph and documentation references in the public skillpack.

- Do not place shared rules here.
- Do not treat this directory as authoritative for cross-project behavior.
- If a rule should be reused, move it into a shared `.codex` asset instead.
