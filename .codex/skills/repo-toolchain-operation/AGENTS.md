# Repo Toolchain Operation Skill Rules

This directory owns the reusable skill for repository toolchain operation.

## Scope

- Applies to `.codex/skills/repo-toolchain-operation/**`.
- Keep this skill narrow: tool selection, invocation boundary, isolation policy.
