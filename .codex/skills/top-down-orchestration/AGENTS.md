# Skill Directory Instructions

- Keep this skill imperative and reusable.
- State when the orchestrator should consult `.codex/graph/**` if present and what to do if it is absent.
- Name the supporting prompt, workflow, and template paths this skill relies on.
- If a lesson repeats, promote it into a shared `.codex` asset instead of leaving it in memory or a one-off plan.
