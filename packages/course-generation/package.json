{
  "name": "@langue/course-generation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@langue/ai": "workspace:*",
    "@langue/generation-contracts": "workspace:*",
    "@langue/generation-registry": "workspace:*",
    "@langue/prompt-catalog": "workspace:*"
  }
}
