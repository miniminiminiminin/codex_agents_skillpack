{
  "name": "@langue/generation-testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@langue/ai": "workspace:*",
    "@langue/content-models": "workspace:*",
    "@langue/generation-contracts": "workspace:*",
    "@langue/generation-registry": "workspace:*",
    "@langue/generation-runtime": "workspace:*",
    "@langue/schemas": "workspace:*",
    "@langue/prompt-catalog": "workspace:*",
    "@langue/prompt-core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.8"
  }
}
